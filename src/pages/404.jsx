
import React from 'react';
import Typography from '@material-ui/core/Typography';

const NotFoundPage = () => (
  <div>
    <Typography variant="display3">NOT FOUND</Typography>
    <Typography variant="subheading">You just hit a route that doesn&#39;t exist... the sadness.</Typography>
  </div>
);

export default NotFoundPage;
